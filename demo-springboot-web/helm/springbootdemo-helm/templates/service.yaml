apiVersion: v1
kind: Service
metadata:
  name: {{ include "springbootdemo-helm.fullname" . }}
  labels:
    {{- include "springbootdemo-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "springbootdemo-helm.selectorLabels" . | nindent 4 }}
