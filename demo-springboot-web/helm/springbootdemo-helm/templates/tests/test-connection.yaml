apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springbootdemo-helm.fullname" . }}-test-connection"
  labels:
    {{- include "springbootdemo-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "springbootdemo-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
